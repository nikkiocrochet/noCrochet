---
label: Review
hide_body: true
display_field: writer
fields:
- name: writer
  type: text
  config:
    required: false
    max: 15
  label: Writer
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: message
  type: textarea
  default: ''
  config:
    required: true
    wysiwyg: false
    schema:
      format: markdown
    max: 50
  label: Message
- name: date
  type: datetime
  label: Date
  description: ''
  config:
    required: false
    date_format: 
    time_format: 
    display_utc: false
  default: now
pages:
- data/reviews/a-cat.toml
- data/reviews/cool-dog.toml

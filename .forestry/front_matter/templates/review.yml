---
label: Review
hide_body: true
display_field: writer
fields:
- name: writer
  type: text
  config:
    required: false
    max: 15
  label: Writer
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: message
  type: textarea
  default: ''
  config:
    required: true
    wysiwyg: false
    schema:
      format: markdown
    max: 50
  label: Message
pages:
- data/reviews/cool-dog.toml
